#include <main.h>
#include <stdio.h>

/**
 * main -print out alphabet in lowercase
 * return: Always (0)
 */

void print_alphabet(void)
{
	char alpha;

	for  (alpha = 'a'; alpha <= 'z'; apha++)
		putchar(alpha);


	putchar("/n");
}
