#include "main.h"

/**
 * _abs- function to compute the absolute value of an integer
 * @a: n represent an integer
 * Return: n if postive integer
 *	-n if negative integer
 *
 */

int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);

}
